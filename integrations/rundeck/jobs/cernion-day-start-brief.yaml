- defaultTab: output
  description: Cernion day-start operator brief
  executionEnabled: true
  id: cernion-day-start-brief
  loglevel: INFO
  name: Cernion Day Start Brief
  nodeFilterEditable: false
  scheduleEnabled: true
  sequence:
    keepgoing: false
    strategy: node-first
    commands:
      - script: |
          integrations/rundeck/scripts/cernion-runbook-call.sh POST /api/operations-runbook/day-start-brief '{"correlationId":"rundeck:${job.execid}","requestedBy":"rundeck:${job.username}"}'
